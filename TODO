keep track of first line of log file so we know if the file is new.
if the file is new, then we should assume we missed some data since
the last time we read it and we should read the next archived log